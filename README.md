# Tips
1. npm install --legacy-peer-deps && npm start (EJECUTAR PARA COMENZAR)
2. npm install -g json-server (PARA INSTALAR EL SERVIDOR DE MANERA GLOBAL SI NO ESTÁ EN EL PC)
3. json-server --watch db.json (PARA QUE EL SERVIDOR COMIENCE A ESCUCHAR, PUEDO ESPECIFICARLE HASTA EL PUERTO...)
